$(function(){
    function setToast() {
        let type;
        let content;

        if(arguments.length === 1) {
            type = 'default';
            content = arguments[0];
        }
    }
});